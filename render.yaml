services:
  - type: web
    name: oracle-master-planner-api
    runtime: python
    buildCommand: "pip install poetry && poetry install"
    startCommand: "uvicorn oracle_master_planner.main:app --host 0.0.0.0 --port 8000"
    envVars:
      - key: PYTHONPATH
        value: /app/src
